<div class='signup'>
  <header>
    <h1>Registrieren</h1>
    <%= image_pack_tag 'logo.svg', alt: 'Logo' %>
  </header>

  <%= render 'shared/errors' %>

  <%= form_with model: @user, url: signup_path do |f| %>
    <div>
      <%= f.text_field :username, placeholder: 'John' %>
      <%= f.label :username %>
    </div>

    <div>
      <%= f.password_field :password, placeholder: '**********' %>
      <%= f.label :password, 'Passwort' %>
    </div>

    <div>
      <%= f.password_field :password_confirmation, placeholder: '**********' %>
      <%= f.label :password_confirmation, 'Passwort bestätigen' %>
    </div>

    <%= f.submit 'Sign Up', class: 'button primary' %>
    <%= link_to 'Zurück', login_path, class: 'button secondary' %>
  <% end %>
</div>
